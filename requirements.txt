# --- Core ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1
requests==2.32.3

# --- Postgres support (optional — only used when DATABASE_URL is postgresql://) ---
# Binary wheel includes libpq, no system packages required.
psycopg[binary]==3.2.3

# --- Dev / test ---
pytest==8.3.3
httpx==0.27.2
